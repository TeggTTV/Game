class EntityCustoms {
    health: number;
    maxHealth: number;
    speed: number;

    constructor(health: number, maxHealth: number, speed: number) {
        this.health = health;
        this.maxHealth = maxHealth;
        this.speed = speed;
    }
}